# 4
mkdir Git4
cd Git4
git init
git clone "repo link"
ls
cd reponame
git branch 
git log
git checkout
git log
git checkout -b branchname
echo "f1 added" >f1. txt 
git add .
git commit -m "f2 added"
git checkout branchname 
git log 
git rebase branchname 
git remote set-url origin "https://tokenlink@repolink"
git push origin branchname
